# プレイテスト手順書 (2026-07 ビルド)

4人プレイテスト+オンライン対戦テスト用のガイドです。配布zipと一緒に読んでください。

---

## 1. 必要なもの

- Windows PC + キーボード (推奨: 有線。無線は入力遅延が出ることがあります)
- 配布された zip ファイル
- (曲データ更新が入った場合のみ) Chrome または Edge

## 2. インストールと起動

1. zip を好きな場所に**解凍**します (例: `C:\Games\pvpharmonics\`)
   - ⚠️ zip のまま実行しない・Program Files 直下は避ける (書き込み権限の問題が出ます)
2. 解凍したフォルダ内の **exe** をダブルクリックで起動
   - 「WindowsによってPCが保護されました」と出たら「詳細情報」→「実行」

## 3. アカウント作成 (初回のみ)

1. 起動するとログイン画面が出ます
2. **「新規登録へ」**からメールアドレス・パスワード・表示名を入力して登録
   - メールは実在しなくてもOK (確認メールは飛びません)。**パスワードは忘れずにメモ**
3. 以後は同じメール/パスワードでログイン。サーバー接続設定は不要 (自動で本番サーバーに繋がります)
4. オフラインでもフリープレイは可能ですが、**対戦とスコア送信にはログインが必要**です

## 4. 曲データについて

- **今回のビルドには全13曲 (song_001〜013) が同梱済み**です。最初は何もしなくてOK
- **譜面の修正**はゲーム起動時に自動でサーバーから同期されます (操作不要)
- **新曲が追加された**とアナウンスがあった場合だけ、以下の「曲同期」を実行してください:

### 曲同期のやり方 (新曲追加時のみ)

1. **Chrome か Edge** で chart-admin を開く: `https://pvpharmonics.duckdns.org/chart-admin/` ※URLとシークレットは主催から共有
2. 画面の **「⬇ Unity 同期」** ボタンを押す
3. フォルダ選択ダイアログで、ゲームの曲フォルダを選ぶ:
   `<解凍先>\<ゲーム名>_Data\StreamingAssets\Songs`
4. 「保存を許可しますか?」→ **許可**。X-Admin-Secret を聞かれたら共有された値を入力
5. ログに「同期が完了しました」と出たら**ゲームを再起動**

## 5. 最初にやる設定 (Config)

タイトルメニューの **CONFIG** から (選曲画面などからは F2 でも開けます)。
操作は画面下のヒントバー参照 (←→でタブ切替、↓で項目へ、↑↓で行移動)。

| 設定 | 場所 | 説明 |
|---|---|---|
| キー割り当て | キー設定タブ | 既定: レーン=**D/F/J/K**、左右の扇形レーン=**S/L**。変えたい場合はキーをクリック→押したいキーを押す |
| ハイスピード | ゲームプレイタブ | ノーツ速度。0.1刻み。まずは 4〜6 あたりから |
| 判定タイミング補正 | ゲームプレイタブ | **音とノーツがズレて感じたらここ**。「キャリブレーション」で自動測定もできます |
| レーン長 | ゲームプレイタブ | レーンの奥行き。見やすい長さに |

> ⚠️ **設定は F5 (保存ボタン) で確定**してください。保存せずに画面を離れると破棄されます。

## 6. フリープレイ

1. タイトル → **PLAY** → 選曲画面
2. ↑↓で曲選択、難易度ボタンで難易度切替、**Space でプレイ開始**、ESC で戻る
3. 判定: ノーツが手前の判定ライン (中央=横線 / 左右=円弧) に重なった瞬間にキーを押す
4. リザルト画面: Space=リトライ / Enter=選曲へ / ESC=タイトル

## 7. オンライン対戦 (1対1)

対戦は **1対1** です。4人の場合は「2組に分かれて同時」または「交代」で回してください。

1. タイトル → PVP (ロビー) → **Space で START** → マッチング開始
2. 2人がキューに入るとマッチ成立 → 対戦前画面で **READY** (制限時間30秒)
3. **曲ピック**: 交互に曲と難易度を選びます (song1=レート低い側、song2=高い側、計2〜3曲)
   - ピックにも制限時間があります。**放置すると没収負け/引き分け**になるので注意
4. プレイ中は相手のスコアがリアルタイム表示されます。セクター (曲の5区間) ごとに勝敗がつき、合計ポイントで勝負
5. 2曲先取で決着 (1-1なら3曲目)。終了後レーティングが増減します

**マッチングされない場合**: 相手がキューに入っているか声をかけ合ってください。片方だけだと待ち続けます。

## 8. 既知の仕様 (不具合ではありません)

- ランキング画面の「COMING SOON」表示 → 未実装の予定機能です
- リザルト左下の「サーバー検証: 準備中」表記 → 表示のみの問題
- BOT-1 / BOT-2 というユーザーが居ることがあります → テスト用の自動プレイヤーです

## 9. 不具合を見つけたら

以下をメモ・スクショして報告してください (スクショ: **Win+Shift+S**):

```
・どの画面で (タイトル / 選曲 / プレイ中 / 対戦ピック中 など)
・何をしたら (押したキー、選んだ曲・難易度)
・何が起きたか (フリーズ / 表示崩れ / 音ズレ / 判定おかしい など)
・発生時刻 (サーバーログと突き合わせるため、だいたいでOK)
・スクショ or 動画
```

**特に見てほしいポイント**:
- 判定タイミングの体感 (補正0のままでズレを感じるか)
- 対戦フロー全体 (マッチング〜ピック〜対戦〜リザルトで詰まる所がないか)
- 曲ごとの譜面と音のズレの有無 (曲名も添えて)

## 10. 困ったとき

| 症状 | 対処 |
|---|---|
| 特定の曲だけ音が鳴らない | §4 の曲同期を実行 → 再起動 |
| サーバーに繋がらない | フリープレイはオフラインで可。対戦は復旧を待つ |
| 画面が固まった | ESC → ダメなら Alt+F4 で再起動。状況を報告してもらえると助かります |
| キーが効かない | Config のキー設定を確認 (F5 保存を忘れていないか) |
